# 基于人体姿态估计与多类型分类模型的视频跌倒检测实验方案

## 1. 实验目标

本实验使用普通摄像头视频完成跌倒与非跌倒二分类。首先利用 MediaPipe BlazePose 从每帧视频中提取 33 个人体关键点，再将连续帧组成姿态序列，分别输入随机森林（RF）、LSTM、时空图卷积网络（ST-GCN）和轻量 Transformer Encoder。四种方法分别代表传统机器学习、循环时序建模、骨架时空图建模和自注意力时序建模。实验重点回答以下问题：

1. 只使用骨架关键点能否有效识别跌倒？
2. LSTM、ST-GCN 和 Transformer 的时序建模是否优于不显式建模时间关系的 RF？
3. 在关键点坐标之外加入速度、躯干倾角等人工运动特征是否有效？
4. 显式利用骨架拓扑的 ST-GCN 是否优于把每帧骨架作为普通向量的 LSTM 和 Transformer？
5. 哪个模型能在准确率、跌倒召回率、模型复杂度和实时性之间取得更好的平衡？

本实验不预先指定最佳模型。RF 作为传统机器学习基线，LSTM 作为经典循环模型，ST-GCN 作为骨架专用模型，轻量 Transformer 作为注意力模型；最终根据 5 折分组交叉验证的折外预测结果得出结论。

## 2. 总体流程

```text
原始视频与标签
      ↓
按人物或原始视频建立5折分组划分
      ↓
BlazePose逐帧提取33个关键点
      ↓
缺失点处理、坐标归一化、序列切窗
      ↓
构建两类输入：坐标特征 / 坐标+运动特征
      ↓
分别训练RF、LSTM、ST-GCN、Transformer
      ↓
逐折在未参与训练的折外视频上评价
      ↓
消融实验、鲁棒性实验、误差分析
```

一个重要原则是：必须先按原始视频或人物分组划分折，再切分滑动窗口。不能先切窗口再随机分折，否则同一段视频中高度相似的相邻窗口可能同时进入训练折和评估折，造成数据泄漏和虚高的结果。

## 3. 实验软硬件环境

### 3.1 推荐环境

- 操作系统：Windows、macOS 或 Ubuntu 均可
- Python：3.10 或 3.11
- 深度学习框架：PyTorch
- 关键点提取：MediaPipe
- 数据处理：NumPy、Pandas、scikit-learn
- 可视化：Matplotlib、Seaborn
- 视频处理：OpenCV
- 可选硬件：NVIDIA GPU；没有 GPU 也可以完成，只是训练时间更长

### 3.2 建议安装的软件包

在项目目录创建独立环境，然后安装：

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install torch torchvision mediapipe opencv-python numpy pandas scikit-learn matplotlib seaborn tqdm pyyaml
```

Windows 激活环境时使用：

```powershell
.venv\Scripts\activate
```

安装后记录 Python、PyTorch、MediaPipe 版本，以及 CPU、GPU 和内存信息。最终报告中的推理速度只有在注明测试硬件时才有比较意义。

## 4. 项目目录设计

建议建立如下目录：

```text
fall_detection/
├── data/
│   ├── raw/                 # 原始视频，只读保存
│   ├── metadata.csv         # 视频路径、人物、场景和标签
│   ├── splits/              # train/val/test清单
│   ├── keypoints/           # 每段视频的关键点序列
│   └── windows/             # 切窗后的模型输入
├── configs/                 # 训练参数配置
├── src/
│   ├── extract_pose.py
│   ├── preprocess.py
│   ├── dataset.py
│   ├── models.py
│   ├── train.py
│   ├── evaluate.py
│   └── realtime_demo.py
├── checkpoints/             # 最佳模型权重
├── results/
│   ├── figures/
│   ├── metrics/
│   └── predictions/
└── requirements.txt
```

原始数据不要手工覆盖或改名。所有处理结果写入新目录，保证实验可以从原始数据重新生成。

## 5. 数据集准备

### 5.1 默认数据集

本方案默认使用 UR Fall Detection Dataset。

### 5.2 数据检查

在训练前统计：

- 跌倒与非跌倒视频数量：30、40；
- 不同人物、场景和视角的数量: 多人物、单个场景单个视角；
- 视频时长、帧率分布：时长为几秒不等、帧率固定为30；
- BlazePose 无法稳定检出人体的视频比例。

随机抽取至少 5 个跌倒视频和 5 个非跌倒视频人工播放，确认标签和视频内容一致。

### 5.3 数据划分

本课程数据只有 **30 个 fall、40 个 non-fall，共 70 个原始样本**，不再额外留出固定测试集。采用 **5 折分层分组交叉验证**：若这 70 个样本各自对应一段独立原始视频，则每折约 6 个 fall、8 个 non-fall 视频作为折外评估集，其余约 24 个 fall、32 个 non-fall 视频用于训练和内部验证。五折轮换后，每个原始样本恰好被折外评价一次。若其中多个样本来自同一个人物或同一段原始视频，必须合并到同一组，实际每折数量可能不再恰好为 6/8。

分组单位优先使用 `subject_id`：同一人物的所有视频必须留在同一折；只有当人物数量和类别分布足以使五折均包含 fall 与 non-fall 时才这样做。否则使用 `video_id` 作为分组单位，让同一原始视频的全部窗口留在同一折，并在报告中明确结果仅表示对**未见视频**的泛化，不能声称对**未见人物**的泛化。采用 `StratifiedGroupKFold(n_splits=5, shuffle=True, random_state=42)`；若人物分组无法实现每折双类别，回退到视频分组。生成折清单后人工核对每折视频数和类别数，不应只凭预期的 6/8 数字。

在每个外层折内，再从该折的约 56 个训练候选视频中按相同分组规则划出约 20% 作为**内部验证集**，用于 RF 参数选择、深度模型早停和概率阈值选择；剩余视频才用于拟合模型。外层评估折在上述选择完成前完全不可见。内部验证集必须同时含有两类视频；若人物分组导致无法满足此条件，使用视频分组并记录限制。

保存 `fold_1.csv`～`fold_5.csv`，每个文件记录 `video_id、subject_id、label、role`，其中 `role` 为 `train/val/outer_test`。四个模型共用完全相同的五折清单；固定随机种子 42，并保存实际划分，不能为某个模型重新分折。所有插值、标准化、类别权重和数据增强只能依据该折训练部分计算或执行，内部验证和外层评估部分不得参与拟合。

最终报告五折分别的 Accuracy、Precision、跌倒 Recall 和 F1，以及**五折均值±标准差**。另外汇总五折的折外预测（每个原始视频仅出现一次）计算总体混淆矩阵。窗口级结果可作为补充；主结果以原始视频或跌倒事件为单位，避免长视频因窗口多而被赋予过大权重。

## 6. BlazePose 关键点提取

### 6.1 提取内容

对每帧提取 33 个关键点，每个点保存：

- `x`：横向归一化坐标；
- `y`：纵向归一化坐标；
- `z`：相对深度；
- `visibility`：关键点可见度。

基础实验建议先用 `(x, y, visibility)`，即每帧 99 维。`z` 的稳定性可能受普通单目视频影响，可在扩展实验中再加入。

建议设置：

- `static_image_mode=False`；
- `model_complexity=1`；
- `smooth_landmarks=True`；
- `min_detection_confidence=0.5`；
- `min_tracking_confidence=0.5`。

### 6.2 逐帧处理步骤

1. RGB图片输入 BlazePose，获得 33 个关键点。
2. 保存当前帧号、时间戳和关键点数组。
3. 保存是否成功检出人体的掩码。
4. 可选地把关键点画回图片。

每段视频保存为一个 `.npz` 文件，其中包括 `keypoints`、`valid_mask`、`fps`、`label` 和 `video_id`。

## 7. 关键点预处理与特征构建

### 7.1 缺失值处理

- 连续缺失不超过 5 帧：对每个坐标做线性插值；
- 视频首尾缺失：使用最近有效帧前向或后向填充；
- 连续缺失超过 5 帧：保留缺失掩码，并将该窗口标为低质量；
- 一个窗口中有效帧少于 70%：训练时剔除，测试时记录为检测失败案例。

不要使用外层评估折或内部验证集的整体统计量填补数据。若需要均值或标准差，只能由当前折的训练部分计算。

### 7.2 坐标归一化

为了降低人物位置和拍摄距离差异，对每帧做身体中心和平移尺度归一化：

1. 左髋与右髋的中点作为身体中心；
2. 所有关键点坐标减去身体中心；
3. 使用肩宽，或肩中心到髋中心的躯干长度作为尺度；
4. 所有坐标除以该尺度；
5. 尺度过小或关键点不可见时沿用最近有效尺度。

设关键点为 `(x_i, y_i)`，髋中心为 `(x_c, y_c)`，尺度为 `s`，则：

```text
x'_i = (x_i - x_c) / (s + ε)
y'_i = (y_i - y_c) / (s + ε)
```

其中 `ε` 可取 `1e-6`。归一化后可将极端值裁剪到 `[-5, 5]`，减少异常检测结果对训练的影响。

### 7.3 输入方案 A：仅关键点坐标

每帧使用 33 个点的 `x'、y'、visibility`，输入维数为：

```text
33 × 3 = 99维/帧
```

这是消融实验中的基础输入。

### 7.4 输入方案 B：坐标与运动特征

在方案 A 上增加：

- 每个关键点相邻帧的横向和纵向速度；
- 肩中心、髋中心和脚踝中心的垂直速度；
- 躯干相对竖直方向的倾斜角；
- 人体包围盒宽高比；
- 头部、肩部和髋部相对画面高度；
- 关键点平均可见度。

相邻帧速度按实际时间计算：

```text
v_t = (p_t - p_(t-1)) × fps
```

躯干倾角使用肩中心到髋中心的向量计算。每折只在该折训练部分拟合人工特征的标准化参数，并把同一均值和标准差用于内部验证集与外层评估折。

### 7.5 序列切窗

统一将视频采样为 30 FPS；若原始帧率不同，按时间戳进行等间隔采样。默认窗口长度为 30 帧，即约 1 秒；步长为 15 帧，即 50% 重叠。

窗口标签规则：

- 如果有逐帧跌倒标注，窗口内跌倒帧比例达到 50% 时标为跌倒；
- 如果只有视频级标签，优先人工补充跌倒发生区间；
- 无法补充帧级区间时，不能把整段跌倒视频的所有窗口都标成跌倒，因为跌倒前的正常活动会引入错误标签。可只截取明确包含跌倒动作的短片段。

窗口长度若另做敏感性实验，可比较 15、30、60 帧。切窗后逐折检查训练、内部验证、外层评估窗口的原视频来源，确保没有跨组。

### 7.6 仅对训练集进行的数据增强

可以采用轻量骨架增强：

- 水平镜像，概率 0.5；
- 坐标高斯噪声，标准差 0.005～0.02；
- 随机缩放 0.9～1.1；
- 时间裁剪或轻微时间伸缩 0.9～1.1；
- 随机遮蔽少量非核心关节点。

增强不能用于内部验证集和外层评估折。镜像时需要交换左右对应关节的编号。

## 8. 模型设计

### 8.1 RF 传统机器学习基线

随机森林（Random Forest，RF）代表论文第 3.2 节的传统机器学习路线。选择 RF 而不是同时训练 RF、KNN、MLP 和 SVM，是为了把课程实验重点放在四类建模思想的比较上，并控制工作量。RF 对小样本、非线性特征和类别混合通常较稳定，也不需要梯度训练。

将 `[窗口长度, 每帧特征数]` 展平为一维向量：

```text
输入[B,T,D] → 展平为[B,T×D] → Random Forest → 跌倒概率
```

默认使用 300 棵决策树，`class_weight="balanced"`，其余参数先采用 scikit-learn 默认值。仅在验证集上搜索：

- `n_estimators`：100、300、500；
- `max_depth`：10、20、None；
- `min_samples_leaf`：1、2、4；
- `max_features`：`sqrt`、0.5。

RF 虽然接收连续帧组成的窗口，但展平后不显式维护循环状态、骨架图或注意力关系，因此用于衡量专门的时空建模是否真正带来提升。

### 8.2 LSTM 循环时序模型

推荐结构：

```text
输入序列[B,T,D] → 1层单向LSTM(hidden_size=128)
        → 最后有效时刻隐藏状态
        → Dropout(0.3) → Linear(2)
```

初始实验使用单向、单层 LSTM，不照搬参考论文的 10 层结构，因为课程数据集较小，深层循环网络容易过拟合。仅在验证集上尝试隐藏维度 64、128、256以及 1、2 层。LSTM 将每帧骨架作为一个特征向量，重点学习“站立—倾斜—下降—倒地”的时间演化，但不显式使用肩、肘、腕等骨骼连接关系。

### 8.3 ST-GCN 骨架时空图模型

将每个关节视为图节点，按照 MediaPipe BlazePose 的骨骼连接建立空间边，并把相邻帧中的同一关节连接为时间边。输入为：

```text
输入[B,C,T,V]
B：批量大小，C：x、y、visibility等通道
T：窗口帧数，V：33个关节
```

课程实验采用轻量化结构，避免直接照搬大型 10 层网络：

```text
ST-GCN Block：3 → 32
→ ST-GCN Block：32 → 64，时间步长2
→ ST-GCN Block：64 → 128，时间步长2
→ 时间维和关节维全局平均池化
→ Dropout(0.3) → Linear(2)
```

每个模块包含空间图卷积、时间卷积、归一化、ReLU 和残差连接。ST-GCN 同时学习关节连接和连续动作，是本实验中最贴合骨架数据结构的方法。

### 8.4 轻量 Transformer Encoder

Transformer 与 LSTM 使用相同的逐帧特征序列 `[B,T,D]`，先把每帧特征映射到 128 维，再加入可学习位置编码：

```text
输入[B,T,D] → Linear(D,128) → 位置编码
→ 2层Transformer Encoder
→ 时间维平均池化
→ Dropout(0.3) → Linear(128,2)
```

默认使用 4 个注意力头、前馈层维度 256、2 个编码器层和 0.3 Dropout。仅在验证集上搜索嵌入维度 64、128，编码器层数 1、2、4。它通过自注意力直接建立不同帧之间的关系，用于检验全局时间依赖是否优于循环状态和局部图卷积。

### 8.5 公平比较要求

- 四个模型使用完全相同的五折分组清单、窗口来源、标签和基础预处理结果；
- 主对比中四个模型都只使用相同的逐关节 `x、y、visibility`。区别仅在张量组织：RF展平，LSTM和Transformer保留时间维，ST-GCN同时保留时间维和关节维；
- LSTM、ST-GCN、Transformer 使用相同的训练轮数上限、早停规则和评价代码；RF 使用相同验证集选择超参数；
- 报告五折指标的均值和标准差，不只报告最好的一折；
- 每折调参只能看该折内部验证集；该折外层评估视频只在配置、早停轮次和阈值确定后评价一次。

## 9. 模型训练

### 9.1 默认训练参数

| 参数 | 默认值 |
|---|---:|
| 批量大小 | 32 |
| 优化器 | AdamW（LSTM、ST-GCN、Transformer） |
| 初始学习率 | 0.001 |
| 权重衰减 | 0.0001 |
| 最大轮数 | 100 |
| 早停耐心值 | 12 |
| 学习率策略 | ReduceLROnPlateau |
| 梯度裁剪 | 1.0（LSTM、Transformer） |
| Dropout | 0.3 |
| 随机种子 | 42（固定，用于复现数据划分和训练） |

### 9.2 类别不平衡处理

先统计当前折训练部分的窗口类别数。如果多数类与少数类比例超过 1.5:1，RF 使用 `class_weight="balanced"`，三个深度模型使用按该折训练部分类别频数计算的加权交叉熵。不要根据内部验证集或外层评估折计算类别权重。

若跌倒类数量很少，也可以对训练集使用加权采样器，但不要同时大幅加权损失和过度采样，以免模型过分偏向跌倒类。

### 9.3 每轮训练流程

RF 不采用按轮训练，使用训练集拟合各候选参数组合并以验证集 F1 选出最佳配置。LSTM、ST-GCN 和 Transformer 的每轮训练流程如下：

1. 模型进入训练模式；
2. 读取一个批次并执行仅训练集使用的数据增强；
3. 前向计算 logits 和加权交叉熵；
4. 反向传播并更新参数；
5. 在验证集计算损失、跌倒召回率和 F1；
6. 以验证集跌倒 F1 为主要保存依据；
7. 若连续 12 轮没有提升则提前停止；
8. 保存最佳权重、参数配置、随机种子和训练日志。

### 9.4 阈值选择

模型输出跌倒概率。最小对比实验统一固定分类阈值为 **0.5**，避免仅约 11 个视频的内部验证集上选阈值不稳定。若后续确需调阈值，只能在每折内部验证集上独立选择，并固定后用于该折外层评估视频；禁止使用外层评估结果选阈值。

## 10. 评价指标与计算单位

### 10.1 分类指标

将跌倒作为正类，计算：

- Accuracy；
- Precision；
- Recall/Sensitivity；
- Specificity；
- F1-score；
- ROC-AUC；
- PR-AUC；
- 混淆矩阵。

跌倒检测应重点报告 Recall，因为假阴性表示真实跌倒没有被发现。同时结合 Precision，防止系统频繁误报警。

### 10.2 窗口级与视频级评价

主结果报告视频级结果：把同一视频的窗口跌倒概率取平均，以固定阈值 0.5 判断视频类别；此规则在训练前确定，四个模型和五折统一使用。窗口级结果只作补充，不能把同一视频的多个窗口当作彼此独立的测试样本。若后续要形成事件报警系统，可再单独设计连续窗口触发规则。

视频级指标可增加：

- 跌倒事件检出率；
- 每小时误报警次数；
- 从跌倒开始到报警的检测延迟。

### 10.3 实时性指标

在同一台设备、批量大小为 1、模型预热 20 次后测试 200 次，报告均值和 95% 分位数：

- BlazePose 单帧耗时；
- 分类器单窗口耗时；
- 端到端 FPS；
- 模型参数量；
- 模型文件大小；
- 峰值内存或显存占用（可选）。

测试时关闭梯度计算。GPU 测时需要在计时前后同步，否则耗时可能不准确。

## 11. 必做实验

### 实验一：模型对比

固定 BlazePose、五折分组清单、30 帧窗口、步长和标签规则，比较 RF、LSTM、ST-GCN 和轻量 Transformer。目的是比较固定窗口特征分类、循环时序建模、骨架时空图建模和自注意力建模。主对比统一使用归一化后的 `x、y、visibility`，只改变数据组织形式和模型结构，避免因输入信息不同破坏公平性。每折产生约 14 个视频级折外预测，最后汇总全部约 70 个视频的预测。

| 模型 | 5折Accuracy均值±标准差 | 5折跌倒Recall均值±标准差 | 5折F1均值±标准差 |
|---|---:|---:|---:|
| RF |  |  |  |
| LSTM |  |  |  |
| ST-GCN |  |  |  |
| Transformer |  |  |  |

### 实验二：特征消融

固定表现最好的深度模型，比较：

1. 仅归一化关键点坐标；
2. 坐标 + visibility；
3. 坐标 + 关键点速度；
4. 坐标 + 速度 + 倾角 + 宽高比等完整特征。

| 输入特征 | Accuracy | Recall | F1 | 结论 |
|---|---:|---:|---:|---|
| 坐标 |  |  |  |  |
| 坐标+可见度 |  |  |  |  |
| 坐标+速度 |  |  |  |  |
| 完整特征 |  |  |  |  |

### 实验三：窗口长度敏感性

固定最佳模型与特征，比较 15、30、60 帧。窗口步长统一设为窗口长度的一半。分析短窗口的低延迟与长窗口的上下文信息之间的权衡。

### 实验四：实时性

在 CPU 上分别测试 RF、LSTM、ST-GCN 和 Transformer；如有 GPU，再补充三个深度模型的 GPU 结果。关键点提取和分类器耗时应分别报告，因为实际系统的主要瓶颈通常来自姿态估计。RF 不报告神经网络参数量，改为报告树数量、总节点数和模型文件大小。

## 12. 建议扩展实验

### 12.1 遮挡鲁棒性

在各折外层评估视频的关键点上模拟遮挡，随机将 10%、20%、30% 的非核心关节点设为缺失，观察 Recall 和 F1 的下降幅度。不要重新训练模型，以衡量真实鲁棒性。

### 12.2 视角或场景泛化

如果元数据支持，可另做留一摄像机视角或场景评价，训练时完全不使用该场景。该扩展实验应与五折主对比分开报告。

### 12.3 `z` 坐标实验

比较 `(x,y)` 与 `(x,y,z)`。若加入 `z` 后结果变差，可以解释为单目深度估计噪声或跨场景尺度不稳定。

## 13. 误差分析方法

从五折折外预测结果中分别选取：

- 5 个正确检出的跌倒；
- 5 个漏检；
- 5 个误报警；
- 5 个正确识别的非跌倒。

把预测概率随时间绘成曲线，并叠加真实标签和报警阈值。逐个观看原视频，记录错误原因：

- 蹲下、坐下、躺下与跌倒相似；
- 跌倒过程太慢或窗口没有覆盖完整动作；
- 人体被遮挡或离开画面；
- 关键点抖动、左右点交换或检测失败；
- 侧视、俯视等视角变化；
- 训练集缺少相似动作；
- 阈值过高或过低。

报告中不要只给总体指标，应展示至少 2 个漏检和 2 个误检案例，并给出有依据的改进建议。

## 14. 结果图表清单

最终至少生成：

1. 整体方法流程图；
2. 原始帧与骨架关键点叠加示例；
3. 归一化前后骨架对比；
4. LSTM、ST-GCN、Transformer 的训练损失曲线；
5. 三个深度模型的验证集 F1 曲线；
6. 最佳模型的混淆矩阵；
7. 模型性能和实时性对比表；
8. 特征消融柱状图；
9. 不同窗口长度结果图；
10. 典型误检、漏检的概率时间曲线。

训练曲线按折展示。混淆矩阵同时显示数量和按真实类别归一化后的百分比。

## 15. 实际操作顺序

### 阶段一：先跑通最小流程

1. 下载并整理数据集，完成 `metadata.csv`。
2. 只选 2 个跌倒视频和 2 个非跌倒视频测试读取。
3. 用 BlazePose 提取关键点并画回视频，肉眼确认骨架基本正确。
4. 完成归一化和 30 帧切窗。
5. 用少量数据拟合 RF，并训练一个小型 LSTM，分别确认传统分类和梯度训练流程可运行。
6. 检查标签、张量形状和评价代码。

这一阶段的目标不是得到高准确率，而是验证端到端流程没有程序错误。

### 阶段二：建立可信基线

1. 生成并冻结五折分组清单，每折另划内部验证视频。
2. 提取全部视频关键点并完成质量统计。
3. 使用仅坐标特征训练 RF，建立传统机器学习基线。
4. 查看混淆矩阵，确认模型不是只预测多数类。
5. 将该结果记录为最基础基线。

### 阶段三：完成核心对比

1. 构建完整运动特征；
2. 分别训练或拟合 RF、LSTM、ST-GCN、Transformer；
3. 根据验证集选择最佳轮次和分类阈值；
4. 逐折在折外视频上统一评价，汇总五折均值、标准差与总体混淆矩阵；
5. 测量参数量、分类器延迟和端到端 FPS。

### 阶段四：完成消融与误差分析

1. 做特征消融；
2. 做窗口长度实验；
3. 有时间再做遮挡或跨视角实验；
4. 整理误检、漏检视频；
5. 输出所有图表和结论。

## 16. 每次实验的记录模板

每次训练建立独立目录并保存：

```text
run_name: stgcn_xyv_w30_seed42
date:
git_commit:                         # 若使用Git
dataset_version:
split_file:
feature_set: full
window_length: 30
stride: 15
model: ST-GCN
hidden_channels: [32, 64, 128]
learning_rate: 0.001
batch_size: 32
seed: 42
best_epoch:
validation_f1:
test_metrics:
checkpoint_path:
notes:
```

实验命名必须能反映模型、特征、窗口长度和随机种子。任何失败实验也要简要记录原因，以免重复踩坑。

## 17. 结果判定标准

不能预先承诺固定准确率，应根据数据集难度和划分方式解释结果。建议使用以下相对标准：

- LSTM、ST-GCN 或 Transformer 的五折平均 F1 高于 RF，且多数折保持一致，可支持“显式时序建模有效”；
- 完整特征的平均 F1 或 Recall 高于仅坐标输入，可支持“运动特征有效”；
- 若 ST-GCN 优于 LSTM，可支持“显式骨架拓扑建模有效”；
- 若 Transformer 在较长窗口上的提升更明显，可支持“全局时间依赖建模有效”；
- 若 RF 性能接近深度模型但延迟和资源占用更低，可支持“小数据场景下传统模型更具性价比”；
- 若 Accuracy 很高但跌倒 Recall 较低，应判定系统仍存在严重漏检问题；
- 若随机窗口划分显著优于按人物或按视频分组的五折结果，应优先相信分组结果，并讨论泛化困难。

## 18. 常见问题与处理方法

### BlazePose 经常检测不到人

先检查人物是否过小、遮挡或出画。可以降低检测阈值到 0.3 做诊断，但不建议直接作为最终设置。保持宽高比缩放视频，不要把人物压扁。

### 训练准确率高，验证准确率低

这是过拟合。优先减少模型规模、增加 Dropout、使用数据增强和早停，而不是继续增加网络层数。

### 模型只预测非跌倒

检查类别分布、窗口标签和混淆矩阵；使用类别加权损失；确认跌倒窗口确实覆盖了动作发生阶段。

### 指标异常地接近 100%

首先排查数据泄漏：相邻窗口、同一视频或同一人物是否跨越当前折的训练、内部验证和外层评估部分；归一化统计量是否错误地使用了全部数据。

### 四个模型结果差异很小

检查窗口是否过短、标签是否过粗，以及运动特征是否已经把问题变得过于简单。差异小也是有效结果，应结合参数量和延迟讨论，而不是刻意调参制造差距。

## 19. 报告中实验部分的推荐结构

1. 实验环境与数据集；
2. 数据划分与预处理；
3. BlazePose 关键点提取方法；
4. 特征构建与序列切分；
5. RF、LSTM、ST-GCN、Transformer 模型结构；
6. 训练设置与评价指标；
7. 模型对比结果；
8. 特征消融与窗口长度实验；
9. 实时性分析；
10. 误检、漏检和局限性讨论。

撰写结论时必须让每个结论对应具体实验数据。例如，不要只写“ST-GCN 效果更好”，而应写清它相对 RF、LSTM 或 Transformer 在哪项指标上提高多少、延迟变化多少，以及五折结果是否稳定。

## 20. 最低完成标准与进阶完成标准

### 最低完成标准

- 完成一个数据集的规范划分；
- 提取 BlazePose 关键点并完成可视化检查；
- 跑通 RF、LSTM、ST-GCN、Transformer；
- 报告 Accuracy、Precision、Recall、F1 和混淆矩阵；
- 完成特征消融与实时性比较；
- 分析至少 4 个错误案例。

### 进阶完成标准

- 增加视频级事件检测与报警延迟；
- 完成窗口长度、遮挡和跨场景实验；
- 报告五折指标的均值与标准差；
- 制作实时摄像头演示程序。

按照以上顺序推进时，应先保证数据划分和标签可信，再追求模型复杂度。对于本课程项目，完整、无泄漏、可复现的实验链路，比堆叠复杂网络更有说服力。
